server:
  port: 8080
spring:
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: Asia/Shanghai
  redis:
    host: 127.0.0.1
    port: 6379
  datasource:
    tomcat:
      initSQL: SET NAMES utf8mb4
    url: jdbc:mysql://127.0.0.1:3306/shengxian-ol?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=UTF-8
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  session:
    store-type: redis
    redis:
      namespace: leiming


mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl







